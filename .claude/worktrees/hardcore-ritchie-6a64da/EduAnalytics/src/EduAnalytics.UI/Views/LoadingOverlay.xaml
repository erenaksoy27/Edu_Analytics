<UserControl x:Class="EduAnalytics.UI.Views.LoadingOverlay"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#B3FFFFFF"
             IsHitTestVisible="True">
    <UserControl.Resources>
        <Storyboard x:Key="SpinAnim" RepeatBehavior="Forever">
            <DoubleAnimation
                Storyboard.TargetName="SpinnerArc"
                Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)"
                From="0" To="360" Duration="0:0:1"/>
        </Storyboard>
    </UserControl.Resources>

    <UserControl.Triggers>
        <EventTrigger RoutedEvent="UserControl.Loaded">
            <BeginStoryboard Storyboard="{StaticResource SpinAnim}"/>
        </EventTrigger>
    </UserControl.Triggers>

    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
        <Grid Width="36" Height="36">
            <!-- Arka halka -->
            <Ellipse Stroke="{StaticResource BorderBrush}" StrokeThickness="3"/>
            <!-- Dönen yay -->
            <Ellipse x:Name="SpinnerArc"
                     Stroke="{StaticResource PrimaryBrush}" StrokeThickness="3"
                     StrokeDashArray="4 6"
                     RenderTransformOrigin="0.5,0.5">
                <Ellipse.RenderTransform>
                    <RotateTransform Angle="0"/>
                </Ellipse.RenderTransform>
            </Ellipse>
        </Grid>
        <TextBlock Text="Yükleniyor..."
                   Margin="0,12,0,0"
                   FontSize="13"
                   Foreground="{StaticResource TextSecondaryBrush}"
                   HorizontalAlignment="Center"/>
    </StackPanel>
</UserControl>
