<UserControl x:Class="EduAnalytics.UI.Views.LearningOutcomeManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:EduAnalytics.UI.ViewModels"
             xmlns:views="clr-namespace:EduAnalytics.UI.Views"
             d:DataContext="{d:DesignInstance Type=vm:LearningOutcomeManagementViewModel}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Başlık -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Text="Öğrenim Çıktısı (ÖÇ) Yönetimi" Style="{StaticResource HeadingStyle}"/>
                <TextBlock Text="Ders bazlı öğrenim çıktılarını kod ve açıklama olarak tanımlayın. Kod alanına yalnızca 1, 2, 3 gibi sıra numarası girmeniz yeterlidir; sistem kayıt sırasında bunları otomatik olarak ÖÇ-1, ÖÇ-2, ÖÇ-3 biçimine çevirir."
                           Foreground="{StaticResource TextSecondaryBrush}"/>
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                <Button Content="Şablon İndir"
                        Command="{Binding DownloadExcelTemplateCommand}"
                        Style="{StaticResource SecondaryButtonStyle}"
                        Margin="0,0,8,0"
                        ToolTip="Kod ve Açıklama kolonlarını içeren boş Excel şablonu indir. Kod alanına 1 yazarsanız sistem ÖÇ-1 olarak kaydeder."/>
                <Button Content="Excel İçe Aktar"
                        Command="{Binding ImportExcelCommand}"
                        Style="{StaticResource PrimaryButtonStyle}"
                        ToolTip="Excel'deki öğrenim çıktılarını ekle veya aynı kod varsa güncelle"/>
            </StackPanel>
        </Grid>

        <!-- Form alanı -->
        <Border Grid.Row="1" Style="{StaticResource CardStyle}">
            <StackPanel>
                <Grid Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock Text="Ders" Style="{StaticResource LabelStyle}"/>
                        <ComboBox ItemsSource="{Binding Courses}"
                                  SelectedItem="{Binding SelectedCourse}"
                                  DisplayMemberPath="Name"/>
                    </StackPanel>
                    <Button Grid.Column="1"
                            Content="Yeni Çıktı"
                            Command="{Binding StartNewCommand}"
                            Style="{StaticResource PrimaryButtonStyle}"
                            VerticalAlignment="Bottom"
                            Margin="12,0,0,0"/>
                </Grid>

                <Border Style="{StaticResource CardSubtleStyle}">
                    <StackPanel>
                        <Grid Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="Kod (örn. 1)" Style="{StaticResource LabelStyle}"/>
                                <TextBox Text="{Binding FormCode, UpdateSourceTrigger=PropertyChanged}"
                                         ToolTip="Bu alana yalnızca sıra numarasını yazmanız yeterlidir. Örneğin 1 girildiğinde kayıt ÖÇ-1, 2 girildiğinde ÖÇ-2 olarak oluşturulur; Excel içe aktarmada da aynı kural uygulanır."/>
                            </StackPanel>
                        </Grid>

                        <TextBlock Text="Açıklama" Style="{StaticResource LabelStyle}"/>
                        <TextBox Text="{Binding FormDescription, UpdateSourceTrigger=PropertyChanged}"
                                 AcceptsReturn="True"
                                 TextWrapping="Wrap"
                                 MinHeight="64"
                                 VerticalContentAlignment="Top"/>

                        <StackPanel Orientation="Horizontal" Margin="0,14,0,0" HorizontalAlignment="Right">
                            <Button Content="İptal"
                                    Command="{Binding StartNewCommand}"
                                    Style="{StaticResource SecondaryButtonStyle}"
                                    Margin="0,0,8,0"/>
                            <Button Content="Kaydet"
                                    Command="{Binding SaveCommand}"
                                    Style="{StaticResource PrimaryButtonStyle}"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- Hata / Başarı -->
                <Border Style="{StaticResource AlertErrorStyle}"
                        Margin="0,12,0,0"
                        Visibility="{Binding ErrorMessage, Converter={StaticResource BoolToVisibility}}">
                    <TextBlock Text="{Binding ErrorMessage}" Foreground="#991B1B" TextWrapping="Wrap"/>
                </Border>
                <Border Style="{StaticResource AlertSuccessStyle}"
                        Margin="0,12,0,0"
                        Visibility="{Binding SuccessMessage, Converter={StaticResource BoolToVisibility}}">
                    <TextBlock Text="{Binding SuccessMessage}" Foreground="#064E3B" TextWrapping="Wrap"/>
                </Border>
            </StackPanel>
        </Border>

        <!-- Liste -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding LearningOutcomes}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource CardStyle}" Padding="16">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="80"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <Border Grid.Column="0" Background="{StaticResource PrimaryLightBrush}" CornerRadius="6" Padding="8,6"
                                        VerticalAlignment="Top" HorizontalAlignment="Left">
                                    <TextBlock Text="{Binding Code}" Foreground="{StaticResource PrimaryBrush}"
                                               FontWeight="Bold" FontSize="14"/>
                                </Border>

                                <StackPanel Grid.Column="1" Margin="14,0">
                                    <TextBlock Text="{Binding Description}"
                                               FontWeight="SemiBold"
                                               FontSize="14"
                                               TextWrapping="Wrap"
                                               Margin="0,4,0,0"/>
                                    <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                                        <TextBlock FontSize="11" Foreground="{StaticResource TextSecondaryBrush}">
                                            <Run Text="{Binding TopicCount}"/>
                                            <Run Text=" konu"/>
                                        </TextBlock>
                                        <TextBlock FontSize="11" Foreground="{StaticResource SubtleBrush}" Margin="6,0">·</TextBlock>
                                        <TextBlock FontSize="11" Foreground="{StaticResource TextSecondaryBrush}">
                                            <Run Text="{Binding QuestionCount}"/>
                                            <Run Text=" soru"/>
                                        </TextBlock>
                                    </StackPanel>
                                </StackPanel>

                                <Button Grid.Column="2"
                                        Content="Düzenle"
                                        Command="{Binding DataContext.StartEditCommand,
                                                  RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}"
                                        Style="{StaticResource SecondaryButtonStyle}"
                                        Padding="12,6"
                                        Margin="0,0,6,0"
                                        FontWeight="SemiBold"/>

                                <Button Grid.Column="3"
                                        Content="Sil"
                                        Command="{Binding DataContext.DeleteCommand,
                                                  RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}"
                                        Style="{StaticResource DangerButtonStyle}"
                                        Padding="10,6"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <views:LoadingOverlay Grid.RowSpan="3"
                              Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibility}}"/>
    </Grid>
</UserControl>
